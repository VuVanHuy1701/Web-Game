@using Do_An.Models
@{
    DoAn_WebEntities dBEntities = new DoAn_WebEntities();
    List<Category> list = dBEntities.Categories.ToList();
}
@foreach (var item in list)
 {
    <li>@Html.ActionLink(item.TenHangMuc, "Index", "Top", new { cateId = item.MaCategory }, null)</li>
 }

